import React from "react";

const Wow = (theme, colorMode) => {
  const hero = (name) => ({
    name: "yy",
    setName: () => {
      console.log(`my name is ${name}`);
    },
  });

  return <div>Wow</div>;
};

export default Wow;
